# Light prompt-surface checklist mapping

This table maps every ratified checklist rule whose applicability includes `compressed` to one named, exact line in the built-in light surface. Multiple rules may share a line when that line preserves each rule's scope, condition, mechanism, and polarity. Rules declared `shared` remain composer-appended byte-for-byte; `not-present` applicability remains absent.

| Checklist ID | Light asset | Named line | Exact light line quote |
| --- | --- | --- | --- |
| G-001 | guidance:primary | L-G-TAGS | Your context is a desk. Every message and tool output lands on it with a §N§ tag as its handle. When an item no longer needs to stay for the work ahead, stamp it: `ctx_reduce` with its tag. Stamping removes nothing — the item stays readable; from time to time, when stamped items have piled up and the desk needs room, Magic Context clears them in one sweep (you only stamp, you don't pick the moment). Stamp as soon as an item has served its purpose, silently; never stamp a user message for what it asks (a used paste inside one is fine). Nothing is lost: a cleared item goes to the archive (a recent one leaves `[dropped §N§]`, an older one nothing) and `ctx_expand(message=N)` brings it back whole. Occasional reminders about unstamped material are housekeeping, not warnings; the desk never gets smaller. |
| G-002 | guidance:primary | L-G-QUEUE | Your context is a desk. Every message and tool output lands on it with a §N§ tag as its handle. When an item no longer needs to stay for the work ahead, stamp it: `ctx_reduce` with its tag. Stamping removes nothing — the item stays readable; from time to time, when stamped items have piled up and the desk needs room, Magic Context clears them in one sweep (you only stamp, you don't pick the moment). Stamp as soon as an item has served its purpose, silently; never stamp a user message for what it asks (a used paste inside one is fine). Nothing is lost: a cleared item goes to the archive (a recent one leaves `[dropped §N§]`, an older one nothing) and `ctx_expand(message=N)` brings it back whole. Occasional reminders about unstamped material are housekeeping, not warnings; the desk never gets smaller. |
| G-003 | guidance:primary | L-G-QUEUE | Your context is a desk. Every message and tool output lands on it with a §N§ tag as its handle. When an item no longer needs to stay for the work ahead, stamp it: `ctx_reduce` with its tag. Stamping removes nothing — the item stays readable; from time to time, when stamped items have piled up and the desk needs room, Magic Context clears them in one sweep (you only stamp, you don't pick the moment). Stamp as soon as an item has served its purpose, silently; never stamp a user message for what it asks (a used paste inside one is fine). Nothing is lost: a cleared item goes to the archive (a recent one leaves `[dropped §N§]`, an older one nothing) and `ctx_expand(message=N)` brings it back whole. Occasional reminders about unstamped material are housekeeping, not warnings; the desk never gets smaller. |
| G-004 | guidance:primary | L-G-QUEUE | Your context is a desk. Every message and tool output lands on it with a §N§ tag as its handle. When an item no longer needs to stay for the work ahead, stamp it: `ctx_reduce` with its tag. Stamping removes nothing — the item stays readable; from time to time, when stamped items have piled up and the desk needs room, Magic Context clears them in one sweep (you only stamp, you don't pick the moment). Stamp as soon as an item has served its purpose, silently; never stamp a user message for what it asks (a used paste inside one is fine). Nothing is lost: a cleared item goes to the archive (a recent one leaves `[dropped §N§]`, an older one nothing) and `ctx_expand(message=N)` brings it back whole. Occasional reminders about unstamped material are housekeeping, not warnings; the desk never gets smaller. |
| G-005 | guidance:primary | L-G-QUEUE | Your context is a desk. Every message and tool output lands on it with a §N§ tag as its handle. When an item no longer needs to stay for the work ahead, stamp it: `ctx_reduce` with its tag. Stamping removes nothing — the item stays readable; from time to time, when stamped items have piled up and the desk needs room, Magic Context clears them in one sweep (you only stamp, you don't pick the moment). Stamp as soon as an item has served its purpose, silently; never stamp a user message for what it asks (a used paste inside one is fine). Nothing is lost: a cleared item goes to the archive (a recent one leaves `[dropped §N§]`, an older one nothing) and `ctx_expand(message=N)` brings it back whole. Occasional reminders about unstamped material are housekeeping, not warnings; the desk never gets smaller. |
| G-006 | guidance:primary | L-G-KEEP-DROP | Your context is a desk. Every message and tool output lands on it with a §N§ tag as its handle. When an item no longer needs to stay for the work ahead, stamp it: `ctx_reduce` with its tag. Stamping removes nothing — the item stays readable; from time to time, when stamped items have piled up and the desk needs room, Magic Context clears them in one sweep (you only stamp, you don't pick the moment). Stamp as soon as an item has served its purpose, silently; never stamp a user message for what it asks (a used paste inside one is fine). Nothing is lost: a cleared item goes to the archive (a recent one leaves `[dropped §N§]`, an older one nothing) and `ctx_expand(message=N)` brings it back whole. Occasional reminders about unstamped material are housekeeping, not warnings; the desk never gets smaller. |
| G-007 | guidance:primary | L-G-KEEP-DROP | Your context is a desk. Every message and tool output lands on it with a §N§ tag as its handle. When an item no longer needs to stay for the work ahead, stamp it: `ctx_reduce` with its tag. Stamping removes nothing — the item stays readable; from time to time, when stamped items have piled up and the desk needs room, Magic Context clears them in one sweep (you only stamp, you don't pick the moment). Stamp as soon as an item has served its purpose, silently; never stamp a user message for what it asks (a used paste inside one is fine). Nothing is lost: a cleared item goes to the archive (a recent one leaves `[dropped §N§]`, an older one nothing) and `ctx_expand(message=N)` brings it back whole. Occasional reminders about unstamped material are housekeeping, not warnings; the desk never gets smaller. |
| G-008 | guidance:primary | L-G-KEEP-DROP | Your context is a desk. Every message and tool output lands on it with a §N§ tag as its handle. When an item no longer needs to stay for the work ahead, stamp it: `ctx_reduce` with its tag. Stamping removes nothing — the item stays readable; from time to time, when stamped items have piled up and the desk needs room, Magic Context clears them in one sweep (you only stamp, you don't pick the moment). Stamp as soon as an item has served its purpose, silently; never stamp a user message for what it asks (a used paste inside one is fine). Nothing is lost: a cleared item goes to the archive (a recent one leaves `[dropped §N§]`, an older one nothing) and `ctx_expand(message=N)` brings it back whole. Occasional reminders about unstamped material are housekeeping, not warnings; the desk never gets smaller. |
| G-009 | guidance:primary | L-G-KEEP-DROP | Your context is a desk. Every message and tool output lands on it with a §N§ tag as its handle. When an item no longer needs to stay for the work ahead, stamp it: `ctx_reduce` with its tag. Stamping removes nothing — the item stays readable; from time to time, when stamped items have piled up and the desk needs room, Magic Context clears them in one sweep (you only stamp, you don't pick the moment). Stamp as soon as an item has served its purpose, silently; never stamp a user message for what it asks (a used paste inside one is fine). Nothing is lost: a cleared item goes to the archive (a recent one leaves `[dropped §N§]`, an older one nothing) and `ctx_expand(message=N)` brings it back whole. Occasional reminders about unstamped material are housekeeping, not warnings; the desk never gets smaller. |
| G-010 | guidance:primary | L-G-TIMING | Your context is a desk. Every message and tool output lands on it with a §N§ tag as its handle. When an item no longer needs to stay for the work ahead, stamp it: `ctx_reduce` with its tag. Stamping removes nothing — the item stays readable; from time to time, when stamped items have piled up and the desk needs room, Magic Context clears them in one sweep (you only stamp, you don't pick the moment). Stamp as soon as an item has served its purpose, silently; never stamp a user message for what it asks (a used paste inside one is fine). Nothing is lost: a cleared item goes to the archive (a recent one leaves `[dropped §N§]`, an older one nothing) and `ctx_expand(message=N)` brings it back whole. Occasional reminders about unstamped material are housekeeping, not warnings; the desk never gets smaller. |
| G-013 | guidance:primary | L-G-RECOVERY | `ctx_search` searches the archive — anything ever said, decided, committed or noted here; ask it before asking the user anything that may already be recorded, phrased as a question with the exact names in it. `<project-memory>` is the pinboard of facts that stay true for the months this work lasts (`#id: fact`), for you and every agent on the project; `ctx_memory` pins what must not have to be found again, especially what cost you turns. `ctx_note` is the tray for work you intend to return to, with its findings attached; findings without an intention are not a note; "take a note" from the user always goes there; a `surface_condition` leaves the note with an outside checker that returns it when the condition holds. |
| G-014 | guidance:primary | L-G-RECOVERY | `ctx_search` searches the archive — anything ever said, decided, committed or noted here; ask it before asking the user anything that may already be recorded, phrased as a question with the exact names in it. `<project-memory>` is the pinboard of facts that stay true for the months this work lasts (`#id: fact`), for you and every agent on the project; `ctx_memory` pins what must not have to be found again, especially what cost you turns. `ctx_note` is the tray for work you intend to return to, with its findings attached; findings without an intention are not a note; "take a note" from the user always goes there; a `surface_condition` leaves the note with an outside checker that returns it when the condition holds. |
| G-021 | guidance:primary | L-G-TIMING | Your context is a desk. Every message and tool output lands on it with a §N§ tag as its handle. When an item no longer needs to stay for the work ahead, stamp it: `ctx_reduce` with its tag. Stamping removes nothing — the item stays readable; from time to time, when stamped items have piled up and the desk needs room, Magic Context clears them in one sweep (you only stamp, you don't pick the moment). Stamp as soon as an item has served its purpose, silently; never stamp a user message for what it asks (a used paste inside one is fine). Nothing is lost: a cleared item goes to the archive (a recent one leaves `[dropped §N§]`, an older one nothing) and `ctx_expand(message=N)` brings it back whole. Occasional reminders about unstamped material are housekeeping, not warnings; the desk never gets smaller. |
| G-022 | guidance:primary | L-G-KEEP-DROP | Your context is a desk. Every message and tool output lands on it with a §N§ tag as its handle. When an item no longer needs to stay for the work ahead, stamp it: `ctx_reduce` with its tag. Stamping removes nothing — the item stays readable; from time to time, when stamped items have piled up and the desk needs room, Magic Context clears them in one sweep (you only stamp, you don't pick the moment). Stamp as soon as an item has served its purpose, silently; never stamp a user message for what it asks (a used paste inside one is fine). Nothing is lost: a cleared item goes to the archive (a recent one leaves `[dropped §N§]`, an older one nothing) and `ctx_expand(message=N)` brings it back whole. Occasional reminders about unstamped material are housekeeping, not warnings; the desk never gets smaller. |
| G-023 | guidance:primary | L-G-KEEP-DROP | Your context is a desk. Every message and tool output lands on it with a §N§ tag as its handle. When an item no longer needs to stay for the work ahead, stamp it: `ctx_reduce` with its tag. Stamping removes nothing — the item stays readable; from time to time, when stamped items have piled up and the desk needs room, Magic Context clears them in one sweep (you only stamp, you don't pick the moment). Stamp as soon as an item has served its purpose, silently; never stamp a user message for what it asks (a used paste inside one is fine). Nothing is lost: a cleared item goes to the archive (a recent one leaves `[dropped §N§]`, an older one nothing) and `ctx_expand(message=N)` brings it back whole. Occasional reminders about unstamped material are housekeeping, not warnings; the desk never gets smaller. |
| G-024 | guidance:primary | L-G-TIMING | Your context is a desk. Every message and tool output lands on it with a §N§ tag as its handle. When an item no longer needs to stay for the work ahead, stamp it: `ctx_reduce` with its tag. Stamping removes nothing — the item stays readable; from time to time, when stamped items have piled up and the desk needs room, Magic Context clears them in one sweep (you only stamp, you don't pick the moment). Stamp as soon as an item has served its purpose, silently; never stamp a user message for what it asks (a used paste inside one is fine). Nothing is lost: a cleared item goes to the archive (a recent one leaves `[dropped §N§]`, an older one nothing) and `ctx_expand(message=N)` brings it back whole. Occasional reminders about unstamped material are housekeeping, not warnings; the desk never gets smaller. |
| G-029 | guidance:subagent | L-G-SUBAGENT-TAGS | Your context is a desk: every message and tool output lands on it with a §N§ tag as its handle. When an item no longer needs to stay for the work ahead, stamp it with `ctx_reduce` ("3-5", "1,2,9"). Stamping removes nothing — the item stays readable until Magic Context clears stamped items in one sweep; newest tags are protected. Stamp as soon as an item has served its purpose, silently; never stamp a user message for what it asks; look at each tag, never blanket-stamp "1-50". |
| G-030 | guidance:subagent | L-G-SUBAGENT-SENTINEL | Your context is a desk: every message and tool output lands on it with a §N§ tag as its handle. When an item no longer needs to stay for the work ahead, stamp it with `ctx_reduce` ("3-5", "1,2,9"). Stamping removes nothing — the item stays readable until Magic Context clears stamped items in one sweep; newest tags are protected. Stamp as soon as an item has served its purpose, silently; never stamp a user message for what it asks; look at each tag, never blanket-stamp "1-50". |
| G-031 | guidance:subagent | L-G-SUBAGENT-DROP | Your context is a desk: every message and tool output lands on it with a §N§ tag as its handle. When an item no longer needs to stay for the work ahead, stamp it with `ctx_reduce` ("3-5", "1,2,9"). Stamping removes nothing — the item stays readable until Magic Context clears stamped items in one sweep; newest tags are protected. Stamp as soon as an item has served its purpose, silently; never stamp a user message for what it asks; look at each tag, never blanket-stamp "1-50". |
| G-032 | guidance:subagent | L-G-SUBAGENT-DROP | Your context is a desk: every message and tool output lands on it with a §N§ tag as its handle. When an item no longer needs to stay for the work ahead, stamp it with `ctx_reduce` ("3-5", "1,2,9"). Stamping removes nothing — the item stays readable until Magic Context clears stamped items in one sweep; newest tags are protected. Stamp as soon as an item has served its purpose, silently; never stamp a user message for what it asks; look at each tag, never blanket-stamp "1-50". |
| G-033 | guidance:primary | L-G-REDUCE-CLOSER | When ctx_reduce is available, use it only as routine housekeeping; never cut task scope or depth because context is large. |
| G-034 | guidance:no-reduce | L-G-NO-REDUCE-CLOSER | When ctx_reduce is unavailable, context is automatic; never prune, heed reduction warnings, or cut task scope or depth because context is large. |
| G-035 | guidance:no-reduce | L-G-NO-REDUCE-RECOVERY | `ctx_search` searches the archive — anything ever said, decided, committed or noted here; ask it before asking the user anything that may already be recorded, phrased as a question with the exact names in it. `<project-memory>` is the pinboard of facts that stay true for the months this work lasts (`#id: fact`), for you and every agent on the project; `ctx_memory` pins what must not have to be found again, especially what cost you turns. `ctx_note` is the tray for work you intend to return to, with its findings attached; findings without an intention are not a note; "take a note" from the user always goes there; a `surface_condition` leaves the note with an outside checker that returns it when the condition holds. |
| T-001 | tool:ctx_reduce | L-T-REDUCE | Stamp an item as no longer needed for the work ahead. Not a delete: stamping QUEUES it, the item stays readable until Magic Context clears stamped items in one sweep, newest tags are protected, and a cleared item goes to the archive (recent: a [dropped §N§] placeholder; older: removed) recoverable via ctx_expand(message=N). The question is "does this need to stay on my desk for what comes next?" — a file you keep editing stays, the grep that found it goes. Stamp used reads/searches/outputs, acted-on build/test output, redundant dumps, extracted pasted payloads; keep user messages, your own text, unresolved errors, raw evidence, exact wording that may matter. Look at each tag; never blanket-stamp "1-50". Grammar: "3-5", "1,2,9", "1-5,8,12-15". |
| T-002 | tool:ctx_reduce | L-T-REDUCE | Stamp an item as no longer needed for the work ahead. Not a delete: stamping QUEUES it, the item stays readable until Magic Context clears stamped items in one sweep, newest tags are protected, and a cleared item goes to the archive (recent: a [dropped §N§] placeholder; older: removed) recoverable via ctx_expand(message=N). The question is "does this need to stay on my desk for what comes next?" — a file you keep editing stays, the grep that found it goes. Stamp used reads/searches/outputs, acted-on build/test output, redundant dumps, extracted pasted payloads; keep user messages, your own text, unresolved errors, raw evidence, exact wording that may matter. Look at each tag; never blanket-stamp "1-50". Grammar: "3-5", "1,2,9", "1-5,8,12-15". |
| T-003 | tool:ctx_reduce | L-T-REDUCE | Stamp an item as no longer needed for the work ahead. Not a delete: stamping QUEUES it, the item stays readable until Magic Context clears stamped items in one sweep, newest tags are protected, and a cleared item goes to the archive (recent: a [dropped §N§] placeholder; older: removed) recoverable via ctx_expand(message=N). The question is "does this need to stay on my desk for what comes next?" — a file you keep editing stays, the grep that found it goes. Stamp used reads/searches/outputs, acted-on build/test output, redundant dumps, extracted pasted payloads; keep user messages, your own text, unresolved errors, raw evidence, exact wording that may matter. Look at each tag; never blanket-stamp "1-50". Grammar: "3-5", "1,2,9", "1-5,8,12-15". |
| T-004 | tool:ctx_reduce | L-T-REDUCE | Stamp an item as no longer needed for the work ahead. Not a delete: stamping QUEUES it, the item stays readable until Magic Context clears stamped items in one sweep, newest tags are protected, and a cleared item goes to the archive (recent: a [dropped §N§] placeholder; older: removed) recoverable via ctx_expand(message=N). The question is "does this need to stay on my desk for what comes next?" — a file you keep editing stays, the grep that found it goes. Stamp used reads/searches/outputs, acted-on build/test output, redundant dumps, extracted pasted payloads; keep user messages, your own text, unresolved errors, raw evidence, exact wording that may matter. Look at each tag; never blanket-stamp "1-50". Grammar: "3-5", "1,2,9", "1-5,8,12-15". |
| T-005 | tool:ctx_reduce | L-T-REDUCE | Stamp an item as no longer needed for the work ahead. Not a delete: stamping QUEUES it, the item stays readable until Magic Context clears stamped items in one sweep, newest tags are protected, and a cleared item goes to the archive (recent: a [dropped §N§] placeholder; older: removed) recoverable via ctx_expand(message=N). The question is "does this need to stay on my desk for what comes next?" — a file you keep editing stays, the grep that found it goes. Stamp used reads/searches/outputs, acted-on build/test output, redundant dumps, extracted pasted payloads; keep user messages, your own text, unresolved errors, raw evidence, exact wording that may matter. Look at each tag; never blanket-stamp "1-50". Grammar: "3-5", "1,2,9", "1-5,8,12-15". |
| T-006 | tool:ctx_expand | L-T-EXPAND | Recover raw conversation behind a <session-history> heading or around a ctx_search hit: ctx_expand(start, end) returns [N] U:/A: lines (~15K-token cap; oversized ranges return the head and where to continue). verbose=true lists messages with per-part previews to pick one; message=N returns that message in full, including a tool output released with ctx_reduce. Ranges after the last compartment are your live tail, not expandable. |
| T-007 | tool:ctx_expand | L-T-EXPAND | Recover raw conversation behind a <session-history> heading or around a ctx_search hit: ctx_expand(start, end) returns [N] U:/A: lines (~15K-token cap; oversized ranges return the head and where to continue). verbose=true lists messages with per-part previews to pick one; message=N returns that message in full, including a tool output released with ctx_reduce. Ranges after the last compartment are your live tail, not expandable. |
| T-008 | tool:ctx_expand | L-T-EXPAND | Recover raw conversation behind a <session-history> heading or around a ctx_search hit: ctx_expand(start, end) returns [N] U:/A: lines (~15K-token cap; oversized ranges return the head and where to continue). verbose=true lists messages with per-part previews to pick one; message=N returns that message in full, including a tool output released with ctx_reduce. Ranges after the last compartment are your live tail, not expandable. |
| T-009 | tool:ctx_expand | L-T-EXPAND | Recover raw conversation behind a <session-history> heading or around a ctx_search hit: ctx_expand(start, end) returns [N] U:/A: lines (~15K-token cap; oversized ranges return the head and where to continue). verbose=true lists messages with per-part previews to pick one; message=N returns that message in full, including a tool output released with ctx_reduce. Ranges after the last compartment are your live tail, not expandable. |
| T-010 | tool:ctx_expand | L-T-EXPAND | Recover raw conversation behind a <session-history> heading or around a ctx_search hit: ctx_expand(start, end) returns [N] U:/A: lines (~15K-token cap; oversized ranges return the head and where to continue). verbose=true lists messages with per-part previews to pick one; message=N returns that message in full, including a tool output released with ctx_reduce. Ranges after the last compartment are your live tail, not expandable. |
| T-011 | tool:ctx_note | L-T-NOTE | Session notes are pending intentions: work you intend to return to, with findings attached ("take a note" always qualifies). Not active steps, an executing plan, or restart insurance; a record of how things stand (world-state, a design at a point in time) with nothing you intend to do about it — that goes stale silently; a fact worth keeping is memory, the rest is nothing. First line = title (<80 chars). write saves; read lists rows (note_ids for bodies); update changes one; dismiss retires 1–50. Dismiss a note when its work lands or is abandoned; a queue you never dismiss from stops being read. surface_condition parks it until an outside check against repo files, git, GitHub or the web holds. |
| T-012 | tool:ctx_note | L-T-NOTE | Session notes are pending intentions: work you intend to return to, with findings attached ("take a note" always qualifies). Not active steps, an executing plan, or restart insurance; a record of how things stand (world-state, a design at a point in time) with nothing you intend to do about it — that goes stale silently; a fact worth keeping is memory, the rest is nothing. First line = title (<80 chars). write saves; read lists rows (note_ids for bodies); update changes one; dismiss retires 1–50. Dismiss a note when its work lands or is abandoned; a queue you never dismiss from stops being read. surface_condition parks it until an outside check against repo files, git, GitHub or the web holds. |
| T-013 | tool:ctx_memory | L-T-MEMORY | Durable facts about this project, shared with every agent on it and kept for the months this work lasts; active ones are already in <project-memory> as `#id: fact`. Write one standalone fact when it must not have to be found again — a rule, an architecture fact, a constraint, a config value, a naming convention — especially what cost you turns. A pending intention with evidence is ctx_note, not memory. write (content + category); update one id (content; category optional); archive one or more ids (reason optional); merge two or more ids (content); get 1–20 ids in any status. |
| T-014 | tool:ctx_memory | L-T-MEMORY | Durable facts about this project, shared with every agent on it and kept for the months this work lasts; active ones are already in <project-memory> as `#id: fact`. Write one standalone fact when it must not have to be found again — a rule, an architecture fact, a constraint, a config value, a naming convention — especially what cost you turns. A pending intention with evidence is ctx_note, not memory. write (content + category); update one id (content; category optional); archive one or more ids (reason optional); merge two or more ids (content); get 1–20 ids in any status. |
| T-015 | tool:ctx_search | L-T-SEARCH | Search the archive — everything that ever happened here that is not on your desk: memories not in <project-memory>, compacted conversation, commits, notes. Phrase query as a natural-language question carrying the exact terms you expect ("where is the opencode source code path?", "why did we choose SQLite over postgres?", "how does the dreamer lease work?") — a keyword stack finds less. Sources (omit for all): memory (rules, conventions), message (compacted conversation; hits carry ordinals for ctx_expand), git_commit (when did this change), note (parked follow-ups), external (cross-project long-term knowledge; explicit calls only). Memory ids alone (`#7234`) resolve directly. from/to restrict every source to an inclusive UTC date range. |
| T-016 | tool:ctx_memory | L-T-MEMORY | Durable facts about this project, shared with every agent on it and kept for the months this work lasts; active ones are already in <project-memory> as `#id: fact`. Write one standalone fact when it must not have to be found again — a rule, an architecture fact, a constraint, a config value, a naming convention — especially what cost you turns. A pending intention with evidence is ctx_note, not memory. write (content + category); update one id (content; category optional); archive one or more ids (reason optional); merge two or more ids (content); get 1–20 ids in any status. |
| T-017 | tool:ctx_search | L-T-SEARCH | Search the archive — everything that ever happened here that is not on your desk: memories not in <project-memory>, compacted conversation, commits, notes. Phrase query as a natural-language question carrying the exact terms you expect ("where is the opencode source code path?", "why did we choose SQLite over postgres?", "how does the dreamer lease work?") — a keyword stack finds less. Sources (omit for all): memory (rules, conventions), message (compacted conversation; hits carry ordinals for ctx_expand), git_commit (when did this change), note (parked follow-ups), external (cross-project long-term knowledge; explicit calls only). Memory ids alone (`#7234`) resolve directly. from/to restrict every source to an inclusive UTC date range. |
